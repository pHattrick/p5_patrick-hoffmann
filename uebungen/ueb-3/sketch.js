function setup() {
  createCanvas(600, 400);

  noStroke();
}

function draw() {
  background(240);

  let a=0;
  let abstand=60;
  let grauwert=100;


  fill(grauwert,100);
  ellipse(a,height/2,90,90);

  //kreis 1
  a+=abstand;
  grauwert=grauwert*1.2;
  fill(grauwert,100)
  ellipse(a,height/2,90,90);

  //kreis 2
  a+=abstand;
  grauwert=grauwert*1.2;
  fill(grauwert, 100)
  ellipse(a,height/2,90,90);

  //kreis 3
  a+=abstand;
  grauwert=grauwert*1.2;
  fill(grauwert, 100)
  ellipse(a,height/2,90,90);

  //kreis 4
  a+=abstand;
  grauwert=grauwert*1.2;
  fill(grauwert, 100)
  ellipse(a,height/2,90,90);

  //kreis 5
  a+=abstand;
  grauwert=grauwert*1.2;
  fill(grauwert, 100)
  ellipse(a,height/2,90,90);

  //kreis 6
  a+=abstand;
  grauwert=grauwert*1.2;
  fill(grauwert, 100)
  ellipse(a,height/2,90,90);

  //kreis 7
  a+=abstand;
  grauwert=grauwert*1.2;
  fill(grauwert, 100)
  ellipse(a,height/2,90,90);

  //kreis 8
  a+=abstand;
  grauwert=grauwert*1.2;
  fill(grauwert, 100)
  ellipse(a,height/2,90,90);

  //kreis 9
  a+=abstand;
  grauwert=grauwert*1.2;
  fill(grauwert, 100)
  ellipse(a,height/2,90,90);

  //kreis 10
  a+=abstand;
  grauwert=grauwert*1.2;
  fill(grauwert, 100)
  ellipse(a,height/2,90,90);

}

function setup() {
  createCanvas(600, 400);

  noStroke();
}

function draw() {
  background(240);

  let a=0;

  fill(0,100);
  ellipse(a,height/2,90,90);

  //kreis 1
  a+=60;
  //a=a+60;
  ellipse(a,height/2,90,90);

  //kreis 2
  a+=60;
  ellipse(a,height/2,90,90);

  //kreis 3
  a+=60;
  ellipse(a,height/2,90,90);

  //kreis 4
  a+=60;
  ellipse(a,height/2,90,90);

  //kreis 5
  a+=60;
  ellipse(a,height/2,90,90);

  //kreis 6
  a+=60;

  ellipse(a,height/2,90,90);

  //kreis 7
  a+=60;
  ellipse(a,height/2,90,90);

  //kreis 8
  a+=60;
  ellipse(a,height/2,90,90);

  //kreis 9
  a+=60;
  ellipse(a,height/2,90,90);

  //kreis 10
  a+=60;
  ellipse(a,height/2,90,90);

}
